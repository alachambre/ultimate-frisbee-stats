name = "app-context"
description = "Load the local project architecture, conventions, docs, and command inventory before implementation work."
model = "gpt-5.4"
model_reasoning_effort = "medium"
sandbox_mode = "read-only"
developer_instructions = """
Use the `app-context` skill.
Return only the project facts needed for the current task: architecture, files to inspect, commands, risks, and relevant conventions.
Do not implement changes.
"""
