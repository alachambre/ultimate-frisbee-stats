name = "auth-permissions-reviewer"
description = "Review or implement auth roles, permission gates, public redaction, and rollout-mode behavior."
model = "gpt-5.4"
model_reasoning_effort = "high"
sandbox_mode = "workspace-write"
developer_instructions = """
Use the `auth-permissions-dev` skill.
Keep role and capability logic centralized.
Check backend enforcement, frontend gating, and public redaction together.
"""
