# Frontend UI Dev Links

These paths are discovery hints. Always verify that a referenced file still exists and inspect the current code before relying on it. If a path, owner, or workflow is stale, update this reference as part of the change.

## Docs

- `AGENTS.md`
- `frontend/README.md`
- `GLOSSARY.md`

## Source

- App shell and routes: `frontend/src/App.tsx`
- Layout: `frontend/src/components/Layout.tsx`
- Shared UI states: `frontend/src/components/shared/`
- Route pages: `frontend/src/pages/`
- Domain components: `frontend/src/components/`
- API services: `frontend/src/services/`
- Query keys: `frontend/src/utils/queryKeys.ts`
- Test helpers: `frontend/src/test/test-utils.tsx`
- MSW handlers: `frontend/src/test/mocks/handlers.ts`
- Locales: `frontend/src/locales/en/`, `frontend/src/locales/fr/`

## Commands

```bash
cd frontend && npm test
cd frontend && npm run build
```
